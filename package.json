{
  "name": "netlify-site-monitor",
  "version": "1.0.0",
  "private": true,
  "description": "Monitor de sites Netlify — detecta sites pausados por limite de uso",
  "scripts": {
    "build": "node -e \"console.log('Static site — no compile step')\"",
    "dev": "netlify dev",
    "test:detector": "node netlify/functions/lib/detectPaused.test.js"
  },
  "devDependencies": {
    "netlify-cli": "^23.0.0"
  }
}
